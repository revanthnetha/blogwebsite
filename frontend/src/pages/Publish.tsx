import React from "react";
import Texteditor from "../components/publish/Texteditor";
import Layout from "../Layout";

const Publish = () => {
  return (
    <div>
      <Layout>
        <Texteditor />
      </Layout>
    </div>
  );
};

export default Publish;
